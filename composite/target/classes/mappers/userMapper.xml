<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.clx.composite.mapper.UserMapper">
    <!--登录-->
    <select id="login" parameterType="requestDTO" resultType="boolean">
        /*查询的值为1时返回true,其他任何值都返回false*/
        <![CDATA[SELECT COUNT(*) FROM user WHERE email=#{email} and password=#{password}]]>
    </select>
    <!--获取用户数据-->
    <select id="listUsers" parameterType="String" resultType="userDO">
        <bind name="queryInfo" value="'%'+_parameter+'%'"/>
        SELECT id,email,username,birthday,sex,registerDay,permission FROM user
        <if test="queryInfo!=''">
            <where>
                id LIKE #{queryInfo}
                OR email LIKE #{queryInfo}
                OR username LIKE #{queryInfo}
                OR birthday LIKE #{queryInfo}
                OR sex LIKE #{queryInfo}
                OR registerDay LIKE#{queryInfo}
            </where>
        </if>
    </select>
    <!--添加用户-->
    <insert id="insertUser" parameterType="requestDTO" >
        <selectKey keyProperty="id" resultType="int" order="AFTER">
            SELECT LAST_INSERT_ID()
        </selectKey>
        INSERT INTO user VALUES(null,#{email},#{password},#{username},#{birthday},#{sex},#{registerDay},#{permission})
    </insert>
    <!--删除用户-->
    <delete id="deleteUsers" parameterType="List">
        DELETE FROM user where id in
        <foreach collection="list" item="idList" open="(" separator="," close=")">
            #{idList}
        </foreach>
    </delete>
    <!--更新用户数据-->
    <update id="updateUser" parameterType="requestDTO">
        UPDATE  user SET
            email=#{email},
            username=#{username},
            birthday=#{birthday},
            sex=#{sex},
            permission=#{permission}
        WHERE
            id=#{id}
    </update>
    <!--判断是否有相同的email存在-->
    <select id="hasRepeatEmail" parameterType="requestDTO" resultType="boolean">
        <![CDATA[select count(*) from user WHERE email=#{email} and id !=#{id}]]>
    </select>
</mapper>